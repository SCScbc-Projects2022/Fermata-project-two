 <section class="mt-5 row align-items-center">
  <div class="container-fluid d-flex mt-5 justify-content-center align-items-center">
    <h1>Welcome to Fermata</h1>
  </div>

  <div class="container-fluid d-flex mt-5 justify-content-center align-items-center">
    <a href="/compose/you">
      <button
        type="button"
        class="btn btn-lg btn-outline-dark"
        id="compose-btn">
        Compose
      </button>
    </a>
  </div>

  <div class="container-fluid d-flex ms-2 mt-5">
    <h4>Drafts:</h4>
  </div>
  {{#if draftLetter.length}}
  <div class="container-fluid ms-2 d-flex">
    <ul>
    {{#each draftLetter as |draft|}}
    <li>
      <a href="/sent/{{draft.id}}">
        <p class="draft d-inline" id="draft-item" style="fontFamily:'{{draft.font.style_tag}}'"> {{!-- fix this font family--}}
          {{draft.updatedAt}} - To {{draft.recipient_name}}
        </p>
      </a>
      <p id="delete-btn" class="d-inline small-text mx-2">
        delete
      </p>
    </li>
    {{/each}}
    </ul>
  </div>
  {{/if}}

  <div class="container-fluid d-flex ms-2 mt-5">
    <h4>History:</h4>
  </div>
  {{#if sentLetter.length}}
  <div class="container-fluid ms-2 d-flex">
    <ul>
    {{#each sentLetter as |sent|}}
    <li>
      <a href="/sent/{{sent.id}}">
        <p class="history" id="history-item">
          {{sent.createdAt}} - To {{sent.recipient_name}}
        </p>
      </a>
    </li>
    {{/each}}
    </ul>
  </div>
  {{/if}}
</section>
<script src="/javascript/dashboard.js"></script>

        {{!-- const draftLetter = draftData.map(draft => draft.get({plain: true}));
        const sentLetter = sentData.map(sent => sent.get({plain: true}));
        res.render('dashboard', {draftLetter, sentLetter, loggedIn: true}); --}}